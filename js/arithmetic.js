function add(){
    let x;// creating a new variable x
    x=6;//assigning a 6 value to x
    let y;// creating a new variable y
    y=1;//assigning a 17 value to y
    let z=x + y;// creating a new vaariable z and addition is done
    document.getElementById('add').innerHTML=z;
}